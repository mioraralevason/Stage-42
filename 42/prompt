
tandrian:

EXEMPLE:
nombre places cluster = 100
nombre places prises = 70
nombre places restantes = 30
nombre places restantes / nombre places cluster = 30 / 100 = 0.3
0.3 * 100 = 30%
Donc 30% de places restantes

    double placesRestantes = cluster.getTotalPlaces() - cluster.getUsedPlaces();
    double pourcentageRestant = (placesRestantes / cluster.getTotalPlaces()) * 100;
    return (int) Math.round(pourcentageRestant);
    